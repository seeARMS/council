{
  "name": "council-web",
  "version": "0.0.0",
  "private": true,
  "description": "Landing page for council.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "npm run build && wrangler dev",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.2.0",
    "tailwindcss": "^4.2.4",
    "wrangler": "^4.86.0"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.3.0"
  },
  "overrides": {
    "vite": "^7"
  }
}
